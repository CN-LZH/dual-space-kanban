请帮我从零搭建一个基于 Vue 3 + Vite + Element Plus 的前端项目。该项目是一个纯前端运行的“双模式个人任务看板系统”，不需要对接任何后端 API。请使用 Vue 3 的 Composition API (<script setup> 语法) 进行开发。

【核心技术要求】
1. 框架与UI：Vue 3, Vite, Element Plus (请引入所有的基础图标)。
2. 路由 (Vue Router)：需要配置两个主要页面：登录页 (/login) 和 看板主页 (/)。
3. 状态管理 (Pinia)：用于全局存储用户登录状态、当前的“空间模式”（工作模式 / 学习模式），以及全部的任务列表数据。请开启 localStorage 数据持久化，刷新页面不丢失数据。

【功能模块详细说明】
1. 登录页面 (Login)：
- 页面居中显示一个美观的 Element Plus 登录卡片 (ElCard)。
- 包含用户名和密码输入框，以及登录按钮。
- 写死前端校验逻辑：只有当用户名为 admin，密码为 123456 时允许登录成功，否则使用 ElMessage 弹出错误提示。
- 登录成功后将状态存入 Pinia 并跳转至首页 (/)。

2. 全局布局 (Layout)：
- 顶部导航栏 (Header)：左侧显示项目名称；中间放置一个显眼的 Switch 开关或 Segmented 控制器，用于全局切换“工作”和“学习”模式；右侧显示当前用户名和一个“退出登录”按钮。
- 模式切换联动：当切换模式时，页面的主题色或背景色需要有轻微的区别（例如工作模式偏蓝灰，学习模式偏暖绿），并且下方的看板数据需要立刻过滤出属于当前模式的任务。

3. 看板主页 (Kanban Board)：
- 页面主体分为三列：待办 (To Do)、进行中 (In Progress)、已完成 (Done)。请使用 Flex 或 Grid 布局。
- 请使用 Element Plus 的 ElCard 渲染每一个任务卡片。
- 支持拖拽交互：请引入 vue-draggable-plus（或其他适用的 Vue 3 拖拽库），实现卡片能在三个状态列之间自由拖拽，拖拽结束后自动更新 Pinia 中的任务状态。
- 新增任务：每列底部有一个“+ 添加任务”按钮，点击后弹出一个 ElDialog 对话框，包含表单：任务标题(必填)、任务描述(选填)、优先级(高/中/低标签)。新增的任务自动归属到当前顶部导航栏选择的模式（工作或学习）中。

4. 初始模拟数据 (Mock Data)：
请在 Pinia 初始化时注入以下结构化的模拟数据，方便我直接看到效果：
- 工作模式下的初始任务："评估旧版 C# WinForms 项目向 Web 端的迁移方案" (待办)、"整理本周工作周报" (已完成)。
- 学习模式下的初始任务："完成 HTML/CSS 翻转卡片特效练习" (进行中)、"使用语音工具进行 30 分钟英语口语交流训练" (待办)。

代码要求模块化，组件拆分清晰，并加上必要的中文注释帮助我理解逻辑。请一步步给出生成和安装依赖的指令，以及完整的源码。